# Secrets and local environment
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets/
credentials/
service-account*.json

# Human-approval markers — local-only evidence of approval, NEVER in git
# (Phase 13 Chunk A1 lesson: the prior `feature/m1-domain` branch accidentally
# committed them via `git add .`. The hooks read them from disk; the repo
# must not carry them.)
.human-approvals/*.txt

# OS/editor
.DS_Store
.idea/
.vscode/
*.swp

# Logs/builds
logs/
*.log
dist/
build/
coverage/
tmp/
.cache/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Node — scoped to top-level so they don't shadow legitimate directory names
# elsewhere in the tree. The unscoped `out/` previously matched
# `src/main/java/.../application/port/out/` and silently dropped 6 ports
# from A2's PR #5; restored in B1.
/node_modules/
/.npm/
/.next/
/out/

# Java
target/
.gradle/
build/
*.iml
*.class
*.jar
*.war

# H2 file-mode data (when implementation lands)
*.mv.db
*.trace.db
*.lock.db
wex-data/

# Windows
Thumbs.db
desktop.ini
