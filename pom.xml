<?xml version="1.0" encoding="UTF-8"?>
<!--
  WEX Purchase Currency Conversion Service.

  M1 (domain layer) pom: plain Maven, no Spring Boot parent yet. M2 (application services)
  introduces the Spring Boot parent when application beans land. M1 stays framework-free.

  Stack ratified in docs/architecture/adr-0001-core-architecture.md D-2 (Phase-4 refined).
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>wex-purchase-fx</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>wex-purchase-fx</name>
    <description>Stores USD purchase transactions and retrieves them converted via the U.S. Treasury Reporting Rates of Exchange API. Out-of-CDE PCI Tier-1 hygiene.</description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Test stack versions (ADR-0001 D-2 ratified) -->
        <junit.jupiter.version>5.11.3</junit.jupiter.version>
        <assertj.version>3.26.3</assertj.version>
        <jqwik.version>1.9.1</jqwik.version>
        <archunit.version>1.3.0</archunit.version>
        <mockito.version>5.14.2</mockito.version>

        <!-- Runtime stack (M1 needs only uuid-creator; the rest enters at M2+) -->
        <uuid.creator.version>5.3.3</uuid.creator.version>

        <!-- Build plugin versions -->
        <surefire.version>3.5.1</surefire.version>
        <jacoco.version>0.8.12</jacoco.version>
        <pitest.version>1.17.0</pitest.version>
        <pitest.junit5.version>1.2.1</pitest.junit5.version>

        <!-- Coverage thresholds (NFR-021; Phase-5 ratified ≥85% on critical classes) -->
        <jacoco.line.coverage>0.85</jacoco.line.coverage>
        <jacoco.branch.coverage>0.75</jacoco.branch.coverage>
        <pitest.mutation.threshold>85</pitest.mutation.threshold>
    </properties>

    <dependencies>
        <!-- Runtime: UUID v7 generator (ADR-0001 D-7; Phase-4 G4-P0-4 closure) -->
        <dependency>
            <groupId>com.github.f4b6a3</groupId>
            <artifactId>uuid-creator</artifactId>
            <version>${uuid.creator.version}</version>
        </dependency>

        <!-- Test: JUnit 5 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Test: AssertJ for fluent assertions -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Test: jqwik for property-based testing (Money invariants) -->
        <dependency>
            <groupId>net.jqwik</groupId>
            <artifactId>jqwik</artifactId>
            <version>${jqwik.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Test: ArchUnit for architectural fitness (no double/float; layer rules) -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Surefire: runs JUnit 5 and jqwik tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.version}</version>
                <configuration>
                    <includes>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                    </includes>
                </configuration>
            </plugin>

            <!-- JaCoCo: line + branch coverage (NFR-021) -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check-domain-coverage</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>com.example.purchaseconversion.domain</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.line.coverage}</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.branch.coverage}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Pitest: mutation testing on RateSelectionPolicy + Money (G4-P1-9) -->
            <plugin>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-maven</artifactId>
                <version>${pitest.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-junit5-plugin</artifactId>
                        <version>${pitest.junit5.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <targetClasses>
                        <param>com.example.purchaseconversion.domain.RateSelectionPolicy</param>
                        <param>com.example.purchaseconversion.domain.Money</param>
                    </targetClasses>
                    <targetTests>
                        <param>com.example.purchaseconversion.domain.RateSelectionPolicyTest</param>
                        <param>com.example.purchaseconversion.domain.MoneyTest</param>
                    </targetTests>
                    <mutationThreshold>${pitest.mutation.threshold}</mutationThreshold>
                    <failWhenNoMutations>true</failWhenNoMutations>
                    <outputFormats>
                        <outputFormat>HTML</outputFormat>
                        <outputFormat>XML</outputFormat>
                    </outputFormats>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
